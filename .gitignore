# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Credenciais da migração Supabase → Firebase (nunca comitar)
serviceAccountKey.json
*serviceAccount*.json
finance-flow-dbc29-f92cb453e333.json
# Nome padrão que o Google Cloud Console usa pra chave de conta de serviço
# baixada (projeto-id + hash) — cobre qualquer chave baixada no futuro também.
*-firebase-adminsdk-*.json
firebase-adminsdk-*.json
finance-flow-dbc29-*.json
*.backup
*.backup.gz
db_cluster*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
